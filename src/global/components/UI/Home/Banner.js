import React from "react";
import styles from "@/styles/banner.module.css";

const Banner = () => {
  return (
    <div className={`${styles.container}`}>
      <h2 className="text-3xl">Hello Banner</h2>
    </div>
  );
};

export default Banner;
